# ft_printf
Custom printf implementation in C from scratch. Handles format specifiers: %c, %s, %p, %d, %i, %u, %x, %X, %%. Features: variadic functions (stdarg.h), recursive algorithms for hex/decimal conversion, memory-safe pointer handling. 42 School project focused on deep understanding of string formatting and low-level output control.
